# Guest authors are looked up here by the `author:` field in page front matter.
# For a single-author site this file is unused - `site.author` from _config.yml
# is used instead. Add guest authors below when needed.
